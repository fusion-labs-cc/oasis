# Python
*.pyc
__pycache__/
/oasis/

# Bundled Windows runtime (local portal bundle: python interpreter + ffmpeg,
# never committed -- so `git pull` auto-updates only source, never the runtime)
/python/
/bin/
/oasis-portal-win64.zip

# Media & temp
*.mp4
*.jpg
*.m3u8
*.DS_Store

# Chrome
chromedriver*

# Aider
.aider*

# PyInstaller (local builds; CI produces the shipped zips)
/build/
/dist/
/oasis-backend-*.zip
# Version stamp written at build time from the git tag (see release.yml).
/VERSION

# Node
node_modules/
.next/

# Misc
queue.txt

# SQLite
*.db
